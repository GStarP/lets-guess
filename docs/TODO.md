# TODO

## 下一步：整体代码优化与公共组件抽取

> 说明：以下为下一阶段计划，仅记录，不在当前阶段实现。

- [ ] 抽取统一的顶部栏组件（`TopBar`），统一高度、边框、标题区与左右操作区布局。
- [ ] 抽取统一的图标按钮组件（`IconButton`），统一尺寸、对齐、hover/disabled 样式。
- [ ] 抽取统一按钮组件（`Button`），支持主要按钮/次要按钮/危险按钮等变体，统一交互反馈。
- [ ] 统一页面中重复的容器与布局模式（如页面壳、滚动区、底部操作区）为可复用组件。
- [ ] 收敛样式设计令牌（颜色、圆角、间距、阴影策略），确保黑白灰风格一致。
- [ ] 将三大页面（出题/游戏/结果）逐步替换为公共组件，减少重复代码与样式分叉。
- [ ] 完成重构后做一轮一致性验收（移动端优先）并执行 `lint/build` 回归验证。
